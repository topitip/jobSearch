query {
  jobs {
    id,
    title,
    company {
      id,
      name,
      logoUrl
    },
    cities {
      id,
      name,
      slug
    },
    locationNames,
    tags {
      id,
      name
    },
    remotes {
      name
    }
  }
}